Hi Tomek, picking the code migration back up. Short version:

•  All custom code is captured in the repo with rollback copies. The VDP bundle is built, but live is back on inline code, so that step gets redone.
•  No first-visit speed gain (June tests) – the value is version history, one-line rollback, tests on every change and cached scripts on repeat visits.
•  Plan: one loader script in the site footer first (every page benefits in one publish), then page by page – VDP, search results, deals, homepage, the rest in two batches. Staging → check → live each time; rollback is one edit.
•  From you: once a page has moved, please don't edit its code embeds in Webflow – send changes to me instead. Nothing else needed.
•  Hosted on my GitHub via CDN (yours is private); portable to your CloudFront later if your engineer wants it.
•  It's mostly automated now, so I'll bill actual time per page rather than quote up front.
