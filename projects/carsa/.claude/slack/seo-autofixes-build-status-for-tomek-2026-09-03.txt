Hi Tomek — the September SEO auto-fixes are done and live on Carsa. All four shipped, plus two extras that turned up during checks.

*What's live*
•  Both stale duplicate vehicle records are off the site. `j16bnt-fa27e` and `f14yeg-03cc0` return 404, the real `j16bnt` and `f14yeg` still return 200. I unpublished rather than deleted, so they're recoverable.
•  `/blog/what-is-adaptive-cruise-control` now redirects to the `-guide` version, which is the one that actually ranks.
•  The extended mechanical warranty page has Product + BreadcrumbList structured data. It had none at all before.
•  Shrewsbury location has been removed the sitemap.

*Two extras*
•  Google's Rich Results Test failed the Product at first — it needs a price, a review or a rating. We added an `AggregateOffer` at £699, which is the "from" price already shown ten times on the page. Now passing with 3 valid items and no errors.
•  After the 301 went live, the old blog URL was still sitting in the sitemap, which Search Console counts as a redirect error. Flagged out and republished.

*One for Carsa*
•  Shrewsbury wouldn't unpublish — Webflow blocked it because a live car, `MJ72RKK`, is still assigned to that branch. Worth asking whether that's intentional, given the branch is meant to be closed. I used the sitemap flag instead, which gets the same result without breaking the link.

*Small thing*
•  The schema breadcrumb points at `/car-care/overview` rather than `/car-care`, since `/car-care` 301s. Keeps a redirect out of the structured data.

Everything is verified against the live site and every change has a documented undo. Nothing here is one-way.
